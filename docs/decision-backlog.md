# Decision Backlog

- Status: Draft
- Owner: Keith / Codex
- Last Updated: 2026-05-12 (Phase 2 整合対応の完了を反映)

## 目的

未決の論点を 1 か所に集約し、次に何を決めるかを迷わないようにする。

同じ論点を別文書で何度も往復しないために、再開時はまずこの一覧を確認する。

## 現在の決定待ち課題

現時点では、`MVP` 完了を止める決定待ち課題はない。

実装中に不足が見つかった場合のみ、この文書へ新しい課題を追加する。

1. `第 2 フェーズ` 以降へ送った公開範囲・監査論点
   - 本人による公開範囲設定を `第 2 フェーズ` 後半で検討する
   - 更新メタ情報の表示は将来の監査画面や管理画面で再検討する
2. 技術的負債として後続管理する論点
   - `tenantId` の DB レベル外部キー制約追加は `PMO_PJPERSONALBASE-36` で管理する
   - `create-hr-admin` の `--fullName` 追加検討は `PMO_PJPERSONALBASE-37` で管理する
3. `AI` 関連の後続論点
   - `AI サマリ` の具体仕様は `フェーズ 4` 詳細設計で詰める
   - `AI 検索 / 推薦` を AI で始めるか通常検索拡張で始めるかは、`フェーズ 4` 着手時にそこまでの実装を見て判断する
4. `退職者 / 休職者 / 論理削除 / ログイン停止` の後続論点
   - 実装時に不足が見つかった場合のみ追加で詰める

## 今回の整理

- `MVP` と `第 2 フェーズ` の実装は完了済みである
- `PMO_PJPERSONALBASE-25` の懸念整理から、将来課題 `PMO_PJPERSONALBASE-36`, `PMO_PJPERSONALBASE-37` を切り出した
- `PMO_PJPERSONALBASE-25` はクローズ済みである
- `PMO_PJPERSONALBASE-26` はクローズ済みである
- Phase 2 完了後の整合対応として、`PMO_PJPERSONALBASE-39` から `44` は完了済みである
- 監査導線は Phase 2 の最終整合対応として、`HR_ADMIN` 向け読み取り専用の監査一覧画面まで実装済みである
- `WorkHistory` の補助「新規作成」は `HR_ADMIN` / `MANAGER` に許可する方針を採る

## 次に着手する推奨順

1. Phase 3 の着手候補と将来課題の優先順位を整理する
2. `PMO_PJPERSONALBASE-36`, `-37` は機能着手とは分けて技術的負債として管理する
3. `AI` 関連の詳細仕様は `フェーズ 4` 着手時に再開する

## 再開時の使い方

- まずこの文書を見て、次に決める課題を 1 つ選ぶ
- 先頭に `MVP 完了を止める課題はない` とある場合は、`current-position.md` と `implementation-plan.md` に従って次フェーズの具体化を進めてよい
- その課題だけを会話で確定する
- 確定後、対応する文書へ反映し、この一覧から削除または更新する
