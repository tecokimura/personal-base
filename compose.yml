services:
  db:
    image: postgres:16-alpine
    container_name: personal-base-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${DB_USER:-postgres}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-postgres}
      POSTGRES_DB: ${DB_NAME:-personal_base}
    ports:
      - "${DB_PORT:-5432}:5432"
    volumes:
      - personal-base-db-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-postgres} -d ${DB_NAME:-personal_base}"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  personal-base-db-data:
