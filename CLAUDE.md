# CLAUDE.md

Claude Code がセッション開始時に自動で読むファイルです。
プロジェクトの役割分担・Backlog 運用・ワークフローを把握してから作業を開始してください。

---

## プロジェクト概要

- 社員管理システム（マルチテナント構成）
- Backend: NestJS + Prisma / Frontend: Next.js / DB: PostgreSQL
- Backlog プロジェクト: `PMO_PJPERSONALBASE`（このプロジェクトのみ操作する）

---

## セッション役割

このプロジェクトは Claude セッションを 2 種類に分けて運用します。

### PM セッション（仕様把握・レビュー担当）

- ユーザーが決めた仕様を `docs/` に反映する
- Backlog に実装タスクを登録し、カテゴリ `実装待ち` を付ける
- 実装完了後のコードレビューを行う
- レビュー結果を Backlog コメントに記録する
- ユーザーへのエスカレーションが必要か判断する

### 実装セッション（Codex）

- カテゴリ `実装待ち` または `修正待ち` の課題を実装する
- 実装完了後は課題を `処理済み` + カテゴリ `レビュー待ち` に更新する
- 仕様が不明確で進められない場合は `処理済み` + カテゴリ `仕様確認待ち` に更新してブロックを明示する
- スコープを独断で広げない。不明点は実装を止めて確認を求める

---

## ワークフロー

```
ユーザーが仕様・方向性を決定
    ↓
PM セッション: docs 更新 → Backlog タスク登録
    （未対応 + カテゴリ: 実装待ち）
    ↓
実装セッション: 実装開始
    （処理中）
    │
    ├─ 仕様が不明確 ──→ 処理済み + 仕様確認待ち
    │                        ↓
    │                   ユーザーが仕様を決定
    │                        ↓
    │                   PM セッション: docs 更新 → 実装再開へ
    │
    └─ 実装完了 ──→ 処理済み + レビュー待ち
                        ↓
                   PM セッション: コードレビュー
                        │
                        ├─ NG → 処理中 + 修正待ち
                        │           ↓
                        │      実装セッション: 修正
                        │           ↓
                        │      処理済み + レビュー待ち（繰り返し）
                        │
                        └─ OK → 処理済み + 動作確認待ち
                                    ↓
                               ユーザーが動作確認
                                    │
                                    ├─ 不具合 → 新規 Backlog 課題 → 実装セッションへ
                                    └─ OK → 完了
```

**バッチ自動実行中はこの図の「PMセッション: コードレビュー」〜「処理済み + 動作確認待ち」までを人間の確認を待たず自動で連結する。** 詳細は [バッチ自動化ポリシー](#バッチ自動化ポリシー) を参照。

---

## Backlog カテゴリ運用

課題にカテゴリを付けてフェーズを表します。カテゴリは `get_categories` MCP ツールで ID を確認してから `update_issue` で更新します。

| カテゴリ | ステータス | 意味 | 次のアクション担当 |
|---|---|---|---|
| `実装待ち` | 未対応 | 仕様確定・実装開始待ち | 実装セッション |
| `レビュー待ち` | 処理済み | 実装完了・コードレビュー待ち | PM セッション |
| `修正待ち` | 処理中 | レビューNG・修正実装待ち | 実装セッション |
| `仕様確認待ち` | 処理済み | 仕様・方向性の判断が必要 | **ユーザー** |
| `動作確認待ち` | 処理済み | レビューOK・ユーザー動作確認待ち | **ユーザー** |

ユーザーが介入すべき課題は `仕様確認待ち` と `動作確認待ち` の 2 カテゴリで絞り込めます。

**バッチ自動実行中の意味の補足**（詳細は [バッチ自動化ポリシー](#バッチ自動化ポリシー)）:
- バッチ実行中は `レビュー待ち`→`動作確認待ち`の遷移がチケットごとに人間の確認を待たず自動で連結される
- `動作確認待ち`は「バッチ終了後にまとめてユーザーが見る」状態を指し、都度の確認は必須ではなくなる
- ヘルスチェックで異常が出たチケットや、レビューNGが解消しなかったチケットは `仕様確認待ち` のまま自動処理を止め、バッチは他のチケットの処理を継続する

---

## 重要ルール

- **仕様・設計の正本は `docs/` に置く。** Backlog は実行管理のみ
- `docs/` と Backlog が矛盾したら `docs/` を優先する
- **スコープ・優先順位・方針選択はユーザー（Keith）が決める**
  - 例外: バッチ自動実行中の自己起票のみ、[バッチ自動化ポリシー](#バッチ自動化ポリシー)で定めた範囲内で実装セッション自身が判断してよい
- 実装セッションは仕様が不明確なまま独断で進めない
- Backlog 操作は `PMO_PJPERSONALBASE` プロジェクトのみ

---

## Git ワークフロー

- ベースブランチは現在のフェーズブランチ（`claude-auto/config.sh` の `IMPL_BRANCH`。現時点では `feat/phase3`）
- フェーズブランチや `develop` / `main` への直接コミット・pushは禁止（`.claude/hooks/guard-git.sh` で機械的にブロックされる）
- 課題ごとに `feat/pmo-{課題番号}-{スラグ}` ブランチを切って作業する
  - スラグは小文字英数字とハイフンのみ、最大30文字
  - バッチ自動実行中は、ベースブランチの代わりにバッチブランチ（`feat/batch-{日付}`）から分岐する。詳細は [バッチ自動化ポリシー](#バッチ自動化ポリシー)
- 実装完了の定義（2026-08時点。詳細背景は PMO_PJPERSONALBASE-110）:
  - 変更したファイルに型エラーが無いことを確認する（`pnpm typecheck` をルートで実行。PMO-111で整備済み）
  - `pnpm test`を実行し、自分の変更によって新たに失敗するテストが増えていないことを確認する（既存の失敗自体の修正は求めない。既存の失敗は PMO_PJPERSONALBASE-110 で別途管理する）
  - `pnpm lint`はESLint未整備のため現時点では必須としない（整備が完了したらこの運用を見直す）
  - 上記を満たさない場合は「レビュー待ち」に更新せず、仕様確認待ちにして止める
- 実装完了後は、ベースブランチ向けの PR を必ず作成する（`gh pr create --base <ベースブランチ>`）。**PRのマージは人間（Keith）のみが行い、実装セッションは絶対にマージしない**
  - バッチ自動実行中は個別チケットではPRを作成せず、バッチブランチへの統合のみ行う。PRはバッチ終了時にバッチブランチからまとめて1本作成する
- 以下は編集・変更禁止（変更が必要な場合は仕様確認待ちにして止める）:
  - `.env*`
  - `.github/workflows/**`
  - 既存の `prisma/migrations/` 配下ファイルの改変（新規マイグレーション追加は可）
- 変更範囲が課題の完了条件に対して大きく広がる場合（目安: 10ファイル/300行超）は、一旦手を止めて仕様確認待ちにする

---

## 参照ドキュメント

| ファイル | 内容 |
|---|---|
| `docs/current-position.md` | 現在地・直近の着手課題 |
| `docs/project-status.md` | プロジェクト全体の状態 |
| `docs/implementation-plan.md` | 実装計画 |
| `docs/roadmap.md` | ロードマップ |
| `claude-auto/collaboration-rules.md` | 役割分担・意思決定ルール |
| `claude-auto/backlog-operation-rules.md` | Backlog 詳細運用ルール（カテゴリ定義の正本） |
| `claude-auto/resume-instructions.md` | セッション再開手順 |

## フェーズ移行チェックリスト

新しいフェーズに移行するときは以下を必ず実施する。PM セッションがフェーズ移行を提案する際はこのリストをユーザーに確認すること。

- [ ] `claude-auto/config.sh` の `IMPL_BRANCH` を新フェーズのブランチ名に変更する（例: `feat/phase4`）
- [ ] 新ブランチを `develop` から作成して push する
- [ ] サーバーを新ブランチで起動し直す
- [ ] 前フェーズのブランチを `develop` にマージする（PR 推奨）
- [ ] Backlog の前フェーズ課題がすべて完了または整理されていることを確認する

---

## claude-auto パイプライン

`claude-auto/` に自動化の土台となるスクリプトと設定を置く。

- **Status: 再設計中**（2026-08時点）。旧cronオーケストレーター（`pipeline.sh`/`diagnose.sh`/`summary.sh`/`verify.sh`）は、無限リトライ・過剰なClaude呼び出し等の問題が見つかったため削除済み。crontabも無効化済み
- 現在は `impl.sh` / `review.sh` を土台として、対話セッション内でバッチ実行する方式（[バッチ自動化ポリシー](#バッチ自動化ポリシー)参照）に作り直し中
- 無人稼働（cron）への再導入は、対話セッションでのバッチ運用が安定してから行う

| ファイル | 内容 |
|---|---|
| `claude-auto/config.sh` | リトライ回数・間隔・Slack webhook などの設定 |
| `claude-auto/impl.sh` | 実装セッション起動スクリプト（バッチブランチ対応） |
| `claude-auto/review.sh` | レビュー（PM）セッション起動スクリプト（バッチブランチ対応） |
| `claude-auto/notify.sh` | Slack 通知スクリプト（将来の無人稼働再導入時に使用） |

---

## バッチ自動化ポリシー

- Status: Active（2026-08-24 導入）
- 目的: チケットごとの人間確認を減らし、まとまった件数を一気に処理してから人間がまとめて見る運用にする

### 基本フロー

1. `feat/phase3` から `feat/batch-{YYYYMMDD}` ブランチを作成する（バッチごとに1本）
2. 対象チケットを1件選び、`feat/pmo-{番号}-{スラグ}` ブランチを **バッチブランチの最新地点から** 作成する
3. 実装 → レビューを人間の確認を待たず自動で連結する
   - レビューNGの場合、自動修正を試みる（**最大2回まで**。それでも解消しなければ次項の失敗扱いへ）
4. 毎チケット、統合前に必ずヘルスチェックを実行する: `pnpm typecheck` と `pnpm test`（リポジトリ全体）、直近ログのエラーパターン確認
   - ヘルスチェックOK かつ レビューOK → チケットブランチをバッチブランチへ統合（マージ）し、チケットブランチを削除。Backlogは `処理済み` + `動作確認待ち` に更新
   - ヘルスチェックNG または レビューNGが解消しない → **バッチブランチへは統合しない**。チケットブランチは残したまま、Backlogを `仕様確認待ち` に更新し理由を記載。バッチは次のチケットへ進む（バッチ全体は止めない）
5. 対象チケットが無くなったら、次の「バッチ中の自己起票」に従って補充するか、バッチを終了する
6. 目標件数（初回10件。以降の運用実績を見て15〜20件に拡大を検討）・トークン予算・ヘルスチェック異常のいずれかに達したらバッチを終了する

### バッチ中の自己起票（スコープの例外）

対象チケットが不足した場合、実装セッションが自らBacklogを起票し実装してよい。範囲は以下の通り厳密に区切る。

**起票・実装してよい範囲:**
- バグ修正、リファクタリング、テスト追加
- 既存画面への小規模な便利機能追加（例: 一覧のソート機能）
- 小さな自動化・利便性向上

**起票してはいけない範囲（対象外・仕様確認待ちにする）:**
- DBスキーマ変更
- 権限・認証・マルチテナント境界に関わる変更
- 新しいページ・画面の新規作成

自己起票したチケットは、件名の先頭に `[AI提案]` を付け、説明欄に起票理由（何を確認していて何が課題だと判断したか）を明記する。

### バッチ終了時の処理

1. バッチブランチ全体で最終ヘルスチェックを実行する
2. バッチブランチに1件も統合されていなければPRを作成せず終了する
3. 統合されたチケットが1件以上あれば、バッチブランチ → `feat/phase3` へ **PRを1本だけ**作成する（個別チケットのPRは作成しない）
4. バッチサマリーを作成し、Backlogのサマリー課題コメントと社内Wikiページの両方に記録する。サマリーに含める内容:
   - 統合できたチケット一覧（課題キー・内容・要約）
   - 統合できず切り離したチケット一覧（理由付き）
   - `[AI提案]` で新規起票したチケット一覧
   - ヘルスチェック結果

### 変わらないルール（バッチ実行中も維持）

- **PRのマージは人間（Keith）のみが行う。実装セッションは絶対にマージしない**（バッチPRも例外なし）
- `.env*` / `.github/workflows/**` / 既存マイグレーションの改変禁止は継続
- スコープ外の自己起票（上記「起票してはいけない範囲」）は行わない
